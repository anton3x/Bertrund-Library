@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Gerar códigos de recuperação de autenticação de dois fatores (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="container mt-4">
    <div class="card border-0 shadow-sm">
        <div class="card-body p-4">
            <h3 class="card-title mb-4">@ViewData["Title"]</h3>

            <div class="alert alert-warning" role="alert">
                <div class="d-flex align-items-center mb-3">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                    <strong>Guarde estes códigos num local seguro.</strong>
                </div>
                <p>
                    Se perder o seu dispositivo e não tiver os códigos de recuperação, perderá o acesso à sua conta.
                </p>
                <p>
                    Gerar novos códigos de recuperação não altera as chaves utilizadas nas aplicações de autenticação.
                    Se pretender alterar a chave utilizada numa aplicação de autenticação, deve
                    <a asp-page="./ResetAuthenticator" class="alert-link">redefinir as suas chaves de autenticação</a>.
                </p>
            </div>

            <div class="mt-4">
                <form method="post">
                    <button class="btn btn-danger" type="submit">
                        <i class="bi bi-shield-lock me-2"></i>
                        Gerar Códigos de Recuperação
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
